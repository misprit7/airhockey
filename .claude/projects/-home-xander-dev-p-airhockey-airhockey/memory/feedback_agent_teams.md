---
name: feedback_agent_teams
description: User prefers agent teams to use split panes mode instead of in-process
type: feedback
---

When spawning agent teammates, use split panes mode instead of in-process (the default).

**Why:** User preference for visibility into agent work.

**How to apply:** When using the Agent tool with `team_name`, the mode should facilitate split pane display rather than running in-process.
